using UnityEngine;
using System.Collections;

public static class RayTools
{
    public enum Axis { X, Y, Z }
    public static bool CrossCast(Vector3 center, float radius, uint rays, Axis axis)
    {
        if (rays == 0) return false;
        return false;
    }
}
